# Acesso ao Spring Configuration Server.
# Precisamos incluir essas configurações em um momento anterior do start da nossa aplicação (bootstrap)
spring:
  application:
    name: 'fornecedor'
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
